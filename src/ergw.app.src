%% -*- erlang -*-
{application, ergw, [
    {vsn,git},
    {description, "3GPP GGSN/PGW"},
    {applications, [kernel, stdlib, lager, erlando, crypto,
		    regine, gen_socket, gtplib, ergw_aaa,
		    setup, exometer_core, netdata, cowboy,
		    compiler, os_mon, jobs]},
    {mod, {ergw_app, []}},
    {registered, []}
]}.
