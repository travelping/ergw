%% -*- erlang -*-
{application, ergw, [
    {vsn, semver},
    {description, "3GPP GGSN/PGW"},
    {applications, [kernel, stdlib, erlando, crypto,
		    regine, gtplib, pfcplib,
		    ergw_aaa, setup, netdata,
		    opentelemetry, opentelemetry_api, opentelemetry_exporter,
		    prometheus, cowboy, prometheus_cowboy,
		    prometheus_diameter_collector,
		    jsx, compiler, os_mon, jobs,
		    riak_core, riak_core_lite_util]},
    {mod, {ergw_app, []}},
    {registered, []}
]}.
