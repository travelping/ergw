%% -*- erlang -*-
{application, ergw, [
    {vsn,git},
    {description, "3GPP GGSN/PGW"},
    {applications, [kernel, stdlib, lager, erlando, crypto,
		    regine, gen_socket, gtplib, pfcplib,
		    ergw_aaa, setup, netdata,
		    prometheus, cowboy, prometheus_cowboy,
		    jsx, compiler, os_mon, jobs]},
    {mod, {ergw_app, []}},
    {registered, []}
]}.
